{
  "name": "ideal-management",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Ideal Management — recruitment, HR and business management site, with the Ideal AI assistant.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "netlify dev",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0",
    "typescript": "~5.8.2"
  }
}
